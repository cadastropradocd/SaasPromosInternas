name = "promos-pages"
compatibility_date = "2024-01-01"

# D1 Database
# Run: npx wrangler d1 create promos-db
# Then replace CHANGE_ME with your actual D1 database ID
[[d1_databases]]
binding = "DB"
database_name = "promos-db"
database_id = "CHANGE_ME"

[vars]
JWT_SECRET = "CHANGE_IN_PRODUCTION"