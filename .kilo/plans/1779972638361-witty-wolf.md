# 🚧 Sprint 3.1-C — Correção SPA Routing (Cloudflare Pages)

## 🎯 Objetivo
Corrigir erro:

```
Unsafe attempt to load URL ... chrome-error://chromewebdata/
```

Garantir que o frontend (React + Vite) funcione corretamente em rotas SPA no Cloudflare Pages.

---

# 🔴 PROBLEMA

Cloudflare Pages NÃO entende rotas do React Router:

Exemplo:
- /login
- /dashboard
- /promotions

Sem configuração → retorna 404 → vira `chrome-error` → quebra app.

---

# 🛠️ SOLUÇÃO

## 1. Criar arquivo `_redirects`

### Caminho obrigatório:

```
apps/web/public/_redirects
````

### Conteúdo EXATO:

```txt
/* /index.html 200
````

---

## 2. Garantir build inclui `_redirects`

Após build:

```bash
cd apps/web
npm run build
```

### Verificar:

```
apps/web/dist/_redirects
```

Se NÃO existir → erro crítico.

---

## 3. Validar vite.config.ts

Arquivo:

```
apps/web/vite.config.ts
```

Deve conter:

```ts
export default defineConfig({
  base: '/',
})
```

---

## 4. Redeploy Cloudflare Pages

Após commit:

* push para GitHub
* aguardar novo deploy

OU:

* manual redeploy no painel

---

# 🧪 TESTES

## Teste 1 — Acesso direto

Abrir no navegador:

```
https://saaspromosinternas.pages.dev/login
```

✔ Deve carregar normalmente
❌ NÃO pode dar erro

---

## Teste 2 — Refresh em rota

1. Acessar `/login`
2. Dar F5

✔ Deve continuar funcionando
❌ NÃO pode quebrar

---

## Teste 3 — Navegação interna

* login → dashboard
* dashboard → outras páginas

✔ tudo funcionando sem erro

---

# ⚠️ POSSÍVEIS ERROS

## Caso ainda falhe:

### 1. `_redirects` não foi para o build

➡️ revisar pasta `public/`

---

### 2. Cloudflare não pegou novo build

➡️ forçar redeploy

---

### 3. Erro JS no frontend

Abrir DevTools:

```
F12 → Console
```

Se existir erro vermelho → corrigir antes

---

# ✅ CRITÉRIOS DE ACEITE

* [ ] /login funciona direto
* [ ] refresh não quebra
* [ ] nenhuma rota retorna 404
* [ ] erro chrome-error desapareceu

---

# 🚀 RESULTADO

Sistema SPA funcionando corretamente em produção:

* Rotas diretas funcionando
* Navegação estável
* Sem erros de frame/browser
